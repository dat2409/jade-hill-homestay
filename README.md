1. In src/config/db, copy index.example.js to index.js, then provide value for <<listening_ip>>, <<port>>, <<database_name>> so it can be appropriate to your mongoose database.
2. Run 'npm install' to install all packages.
